import React from 'react';
import { StyleSheet, Text, View, Button } from 'react-native';

export default function Epic(props) {
  return (
      <View>
          {/* Something for props children. Should be individual
            GoalItem components.
            An epic should contain:
            Title:
            Description:
            Start Date:
            Due Date:
            1 or more GoalItem components
            */}
      </View>
  )
};

const styles = StyleSheet.create({
    container: {
        flex:1,
    },
});